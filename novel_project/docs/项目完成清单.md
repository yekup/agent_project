# Novel-GraphRAG 项目完成清单

> 最后更新: 2026-06-30
> 项目路径: `d:/AIGC_1/novel-graphrag/`

---

## 一、核心功能模块

### 1.1 文档解析引擎 `core/document_parser.py`

| 功能 | 状态 | 说明 |
|------|------|------|
| `DocumentRouter` 统一入口 | ✅ 完成 | 自动检测格式，分发到对应 Parser |
| `TxtParser` TXT 解析 | ✅ 完成 | 自动编码检测(UTF-8/GBK/GB18030)、章节边界提取、杂质过滤 |
| `extract_chapters()` 章节检测 | ✅ 完成 | 支持中文数字/英文 Chapter/特殊标记 |
| 架构预留 | ✅ 完成 | `DocxParser` / `PdfParser` / `MarkdownParser` 接口已定义，未实现 |

### 1.2 层级分块引擎 `core/chunker.py`

| 功能 | 状态 | 说明 |
|------|------|------|
| `NovelChunker` 四层级分块 | ✅ 完成 | book → volume → chapter → paragraph |
| 滑窗 + 重叠策略 | ✅ 完成 | chunk_size / overlap 可配置，段落边界感知切割 |
| 长段落二次分割 | ✅ 完成 | 超过 chunk_size 的段落按句号/对话边界分拆 |
| 过小块尾部合并 | ✅ 完成 | 低于 min_chunk 的块自动合并到前一块 |
| 向量索引 `VectorStoreIndexer` | ✅ 完成 | ChromaDB 批量写入 + 语义搜索 |

### 1.3 Wiki 编译管道 `core/chapter_parser.py`

| 功能 | 状态 | 说明 |
|------|------|------|
| 单章 LLM 提取 | ✅ 完成 | LLM 提取人物/事件/关系/摘要 |
| **长章分块编译（替换 [:3000]）** | ✅ 完成 | 2800 字阈值，语义分块，多块提取后 LLM 合并 |
| **语义分块（段落/对话边界）** | ✅ 完成 | `_semantic_split()` 按段落分割，不打断完整剧情 |
| **子块级断点** 🆕 | ✅ 完成 | `SubChunkCache` 每子块落地缓存，崩溃只重跑未完成块 |
| **冲突规整（多块合并）** 🆕 | ✅ 完成 | `_llm_merge_chunks()` LLM 统一称谓/消弭冲突关系，回退到简单合并 |
| **实体校验** 🆕 | ✅ 完成 | `_validate_entity()` 过滤空名/纯符号/自指关系 |
| LLM 重试（指数退避） | ✅ 完成 | 429/5xx 退避 2^N 秒重试 |
| **Token 预算控制** 🆕 | ✅ 完成 | `TokenBudget` 全程追踪，200 万 token 阈值自动终止 |
| **增量编译模式** 🆕 | ✅ 完成 | `build_wiki_incremental()` 只处理新增/修改章节 |
| **失败章节隔离** 🆕 | ✅ 完成 | `FailedChaptersManager` 独立记录失败，不阻塞流程 |
| 并发控制 | ✅ 完成 | `ThreadPoolExecutor` 最大 3 并发 |
| 断点续传（chapter_id） | ✅ 完成 | `CheckpointManager` 用 chapter_index 集合而非长度 |
| 原子写入 + 自动备份 | ✅ 完成 | `_atomic_write()` 临时文件 → `os.replace` |
| 配置外置 | ✅ 完成 | `compiler_config.py` 所有阈值集中管理 |

### 1.4 知识图谱构建 `core/knowledge_graph.py`

| 功能 | 状态 | 说明 |
|------|------|------|
| `merge_characters()` 跨章合并 | ✅ 完成 | 按名称聚合出场章节/描述/提及次数 |
| `merge_relationships()` 跨章合并 | ✅ 完成 | 聚合关系边权重/章节列表 |
| `build_graph()` NetworkX 构建 | ✅ 完成 | 节点属性 + 边属性 |
| `save_graph()` / `load_graph()` | ✅ 完成 | JSON 序列化/反序列化 |
| **实体消歧/别名聚类** 🆕 | ✅ 完成 | `_build_alias_map()` 基于共现 + 角色相似度自动合并别名 |

### 1.5 三级检索 `core/retriever.py`

| 功能 | 状态 | 说明 |
|------|------|------|
| Level 1 Wiki 检索 | ✅ 完成 | 全书/卷/章节三级匹配 + 动态 top_k |
| Level 2 知识图谱检索 | ✅ 完成 | 人物模糊匹配 + 邻接关系 |
| **Level 3 向量检索** 🆕 | ✅ 完成 | ChromaDB 语义搜索，集成 `VectorStoreIndexer` |
| 三级统一检索 `search()` | ✅ 完成 | 三者结果合并输出 |

### 1.6 多 Agent 系统 `core/agents/`

| 功能 | 状态 | 说明 |
|------|------|------|
| `Coordinator` 协调员 | ✅ 完成 | 意图识别 → 任务拆解 → 多轮调度 → 汇总 |
| `Researcher` 研究员 | ✅ 完成 | 多源检索（Wiki/图谱/原文） |
| `Writer` 撰稿 | ✅ 完成 | 基于检索资料生成报告 |
| `Reviewer` 审核 | ✅ 完成 | 质量评分 + 反馈 |
| `StructuredWriter` 结构化撰稿 🆕 | ✅ 完成 | JSON Schema 强制输出，含 `chapter_id` + `source_snippet` 锚点校验 |

### 1.7 LLM 调用层 `core/llm.py`

| 功能 | 状态 | 说明 |
|------|------|------|
| `call_llm()` 统一调用 | ✅ 完成 | OpenAI SDK 封装，支持 DeepSeek |
| `call_llm_stream()` 流式调用 | ✅ 完成 | SSE 流式输出 |
| API key 缺失回退 | ✅ 完成 | 返回 fallback 消息 |

---

## 二、安全与认证

### 2.1 权限系统 `core/security.py`

| 功能 | 状态 | 说明 |
|------|------|------|
| `JWTHandler` JWT 签发/验证 | ✅ 完成 | 持久密钥（配置文件/环境变量） |
| `PermissionMiddleware` 中间件 | ✅ 完成 | 4 角色权限（admin/editor/viewer/api） |
| `require_role()` 装饰器 | ✅ 完成 | 路由级权限控制 |
| `FileValidator` 文件上传校验 | ✅ 完成 | MIME 白名单 + 文件名安全 + 大小限制 |
| `TokenBucket` 令牌桶限流 | ✅ 完成 | 按用户/IP/接口三级 |
| `SanitizeLogFilter` 日志脱敏 | ✅ 完成 | 自动过滤 API Key/密码/Token |
| `load_config()` 配置加载 | ✅ 完成 | YAML + 环境变量覆盖 |

### 2.2 Auth API `web/routes/auth_routes.py`

| 功能 | 状态 | 说明 |
|------|------|------|
| `POST /api/auth/login` | ✅ 完成 | JWT 登录 |
| `POST /api/auth/register` | ✅ 完成 | 用户注册 |
| `GET /api/auth/me` | ✅ 完成 | 当前用户信息 + 权限列表 |
| `GET /api/auth/users` | ✅ 完成 | 用户列表（仅 admin） |
| 权限列表 `get_permissions()` | ✅ 完成 | 角色 → permissions dict |

---

## 三、Web 前端

### 3.1 设计语言（新科技主题）

| 功能 | 状态 | 说明 |
|------|------|------|
| 深色科技主题 `#0c1222` | ✅ 完成 | bg-tech-bg |
| 青蓝色主色调 `#22d3ee` | ✅ 完成 | tech-main |
| Glass 玻璃拟态效果 | ✅ 完成 | backdrop-blur + rgba |
| Glow 发光阴影 | ✅ 完成 | 0 0 18px rgba |
| Font Awesome 图标 | ✅ 完成 | 导航/按钮/卡片全图标化 |
| 渐变标题效果 | ✅ 完成 | bg-gradient-to-r |

### 3.2 首页 `dashboard.html`

| 功能 | 状态 | 说明 |
|------|------|------|
| 导航栏（权限驱动） | ✅ 完成 | `data-perm` 属性控制显隐 |
| 用户信息/登录登出 | ✅ 完成 | auth.js 集成 |
| 功能卡片 | ✅ 完成 | 上传/图谱/问答/已入库 |
| 使用流程说明 | ✅ 完成 | 4 步流程展示 |
| API 接入状态 | ✅ 完成 | 自动加载 novels 数量 |

### 3.3 图谱页 `graph.html` — 核心交互页面

| 功能 | 状态 | 说明 |
|------|------|------|
| Cytoscape.js 渲染引擎 | ✅ 完成 | 替代 vis-network |
| **双布局引擎** | ✅ 完成 | cose 全图聚类 + concentric 聚焦同心圆 |
| **自适应布局参数** 🆕 | ✅ 完成 | 后端根据节点数动态计算斥力/重力/边长 |
| **布局后强制分离迭代** | ✅ 完成 | 40 轮扩展，确保节点不重叠 |
| **缩放联动标签显隐** | ✅ 完成 | text-opacity 控制，不占位，自适应阈值 |
| **z-index 大节点置底** | ✅ 完成 | 大节点不会遮挡小节点 |
| **局部过滤（minDegree）** | ✅ 完成 | 默认度 >= 3 |
| **孤立节点切换** | ✅ 完成 | 「仅关联实体/显示全部节点」 |
| **聚焦模式分层淡化** | ✅ 完成 | 中心 1.0 → 一级 0.8 → 二级 0.25 |
| 人物搜索聚焦 | ✅ 完成 | 输入名称 → 搜索 → 聚焦展示子图 |
| 角色筛选（主角/配角） | ✅ 完成 | 按角色类型过滤 |
| 全图重置 | ✅ 完成 | 回到全量图谱 |
| 点击节点详情面板 | ✅ 完成 | 浮动覆盖右上，关联分页 |
| 节点悬停 tooltip | ✅ 完成 | 显示角色/关联数/出场数 |
| 画布边距保护 | ✅ 完成 | fit() 带 50~70px padding |
| 窗口 resize 自适应 | ✅ 完成 | 自动 refit + recenter |
| 图例 | ✅ 完成 | 4 种关系颜色说明 |

#### 图谱导出

| 功能 | 状态 | 说明 |
|------|------|------|
| **PNG 高清导出** 🆕 | ✅ 完成 | `cy.png({scale, bg})` 原生 4K |
| **SVG 导出** 🆕 | ✅ 完成 | 内嵌 PNG 快照，可无限缩放 |
| **2x/4x 分辨率** 🆕 | ✅ 完成 | 选项面板可选 |
| **深色/白色背景** 🆕 | ✅ 完成 | 导出时临时切换 |
| **包含图例** 🆕 | ✅ 完成 | 导出图叠加关系图例 |

### 3.4 上传页 `upload.html`

| 功能 | 状态 | 说明 |
|------|------|------|
| 文件选择（多格式） | ✅ 完成 | TXT（Word/PDF 可扩展） |
| 上传 + 解析按钮 | ✅ 完成 | 上传 Progress 反馈 |
| **解析结果展示** 🆕 | ✅ 完成 | 章节数/语义块/格式/大小四格展示 |
| 选择章节数 | ✅ 完成 | 输入 N 章或全量编译 |
| 编译进度条 | ✅ 完成 | 进度百分比展示 |

### 3.5 问答页 `chat.html`

| 功能 | 状态 | 说明 |
|------|------|------|
| 聊天区域 | ✅ 完成 | 用户/助理气泡消息 |
| SSE 流式进度 | ✅ 完成 | 进度条 + 状态文字 |
| 示例问题快捷入口 | ✅ 完成 | 4 个预置问题按钮 |
| 清空对话 | ✅ 完成 | 一键清除 |

### 3.6 登录页 `login.html`

| 功能 | 状态 | 说明 |
|------|------|------|
| 登录表单 | ✅ 完成 | 用户名 + 密码 + 登录按钮 |
| JWT Token 存储 | ✅ 完成 | localStorage 持久化 |
| 错误提示 | ✅ 完成 | 服务端错误展示 |
| 默认管理员提示 | ✅ 完成 | 密码取 ADMIN_PASSWORD 环境变量，未设置时随机生成并打印到控制台 |

### 3.7 前端权限 `static/js/auth.js`

| 功能 | 状态 | 说明 |
|------|------|------|
| 页面加载自动初始化 | ✅ 完成 | DOMContentLoaded |
| `fetchPermissions()` 获取权限 | ✅ 完成 | GET /api/auth/me |
| `applyPermissions()` 显隐元素 | ✅ 完成 | data-perm 属性控制 display |
| 用户信息展示 | ✅ 完成 | 导航栏 username(role) |
| 登录/登出按钮切换 | ✅ 完成 | 有 token 显示登出，否则登录 |
| `setToken()` / `logout()` / `clear()` | ✅ 完成 | 完整的 auth 生命周期 |

---

## 四、API 路由

### 4.1 认证

| 路由 | 状态 | 说明 |
|------|------|------|
| `POST /api/auth/login` | ✅ 完成 | |
| `POST /api/auth/register` | ✅ 完成 | |
| `GET /api/auth/me` | ✅ 完成 | |
| `GET /api/auth/users` | ✅ 完成 | |

### 4.2 图谱

| 路由 | 状态 | 说明 |
|------|------|------|
| `GET /api/graph?novel=` | ✅ 完成 | **含自适应布局参数** |
| `GET /api/novels` | ✅ 完成 | |

### 4.3 检索

| 路由 | 状态 | 说明 |
|------|------|------|
| `POST /api/search` | ✅ 完成 | Wiki + 图谱检索 |
| `POST /api/search/vector` | ✅ 完成 | 语义向量检索 |
| `POST /api/search/all` | ✅ 完成 | 三级统一检索 |

### 4.4 问答

| 路由 | 状态 | 说明 |
|------|------|------|
| `POST /api/ask` | ✅ 完成 | 多 Agent 问答 |
| `POST /api/ask/stream` | ✅ 完成 | SSE 流式 |

### 4.5 上传与编译

| 路由 | 状态 | 说明 |
|------|------|------|
| `POST /api/upload` | ✅ 完成 | 支持多格式，返回分块信息 |
| `POST /api/build` | ✅ 完成 | 编译 Wiki，**返回失败章节列表** |
| `GET /api/build/failed` 🆕 | ✅ 完成 | 查看失败章节清单 |
| `POST /api/build/retry` 🆕 | ✅ 完成 | 单章重试 |
| `POST /api/index` | ✅ 完成 | 向量索引 |

### 4.6 基础

| 路由 | 状态 | 说明 |
|------|------|------|
| `GET /health` | ✅ 完成 | 健康检查 |

### 4.7 MCP Server（独立运行）

| Tool | 状态 | 说明 |
|------|------|------|
| `list_novels` | ✅ 完成 | |
| `search_novel_graph` | ✅ 完成 | |
| `get_character_timeline` | ✅ 完成 | |
| `analyze_chapter` | ✅ 完成 | |
| `search_wiki` | ✅ 完成 | |

---

## 五、生态导出 `core/exporter.py`

| 导出格式 | 状态 | 说明 |
|---------|------|------|
| Obsidian 知识库（双向链接） | ✅ 完成 | 人物/卷/章节三级目录 |
| EPUB 电子书 | ✅ 完成 | ebooklib |
| HTML（EPUB 降级） | ✅ 完成 | |
| Excel 人物+关系清单 | ✅ 完成 | openpyxl |
| CSV（Excel 降级） | ✅ 完成 | |
| Markdown 分析报告 | ✅ 完成 | 含统计+Top10+卷摘要 |

---

## 六、RAG 质量评估 `scripts/rag_evaluate.py`

| 功能 | 状态 | 说明 |
|------|------|------|
| `NovelRAGEvaluator` 评估器 | ✅ 完成 | |
| `FaithfulnessMetric` 忠实度 | ✅ 完成 | LLM-as-Judge |
| `AnswerRelevancyMetric` 相关性 | ✅ 完成 | |
| 黄金测试集生成 | ✅ 完成 | 自动生成 25+ QA 对 |
| 回归门禁 | ✅ 完成 | `--gate --threshold 0.75` |
| CLI 命令行 | ✅ 完成 | 支持 --query / --dataset / --gate |

---

## 七、预留接口 `interfaces/`

| 接口 | 文件 | 所需资源 | 状态 |
|------|------|---------|------|
| 角色立绘 | `portrait_generator.py` | GPU + Stable Diffusion | 🟡 接口已定义 |
| RLHF 微调 | `rlhf_pipeline.py` | GPU + TRL | 🟡 接口已定义 |
| 本地 LLM | `llm_provider.py` | GPU + Qwen-7B | 🟡 DeepSeekProvider 可用，LocalModelProvider 桩 |
| 版权校验 | `copyright_verifier.py` | 起点/晋江 API | 🟡 默认全放通 |
| 图存储 | `graph_storage.py` | Neo4j / TimescaleDB | 🟡 Neo4jBackend 代码完整 |
| 评估指标 | `eval_metrics.py` | — | 🟡 Faithfulness 可用 |

---

## 八、部署与运维

| 功能 | 状态 | 说明 |
|------|------|------|
| `Dockerfile` | ✅ 完成 | Python 3.11-slim |
| `docker-compose.yml` | ✅ 完成 | 6 服务（App/Neo4j/ChromaDB/Redis/Celery/Nginx） |
| `nginx.conf` | ✅ 完成 | 反向代理 + SSE + 静态缓存 |
| `config.yaml` | ✅ 完成 | 统一配置 + 环境变量覆盖 |
| `.env.template` | ✅ 完成 | 环境变量模板 |
| `requirements.txt` | ✅ 完成 | 完整依赖清单 |
| `.gitignore` | ✅ 完成 | Python/IDE/OS/数据/日志 |
| 健康检查 `/health` | ✅ 完成 | |
| Loguru 结构化日志 🆕 | ✅ 完成 | JSON 格式 + 轮转 + 脱敏 |
| 虚拟环境 `.venv` | ✅ 完成 | |

---

## 九、测试

| 测试 | 状态 | 说明 |
|------|------|------|
| 分块引擎测试 | ✅ 完成 | 3 个测试 |
| 文档解析测试 | ✅ 完成 | 2 个测试 |
| 安全模块测试 | ✅ 完成 | 3 个测试 |
| 导出模块测试 | ✅ 完成 | 1 个测试 |
| Auth 权限测试 | ✅ 完成 | 2 个测试 |
| **总计** | **11/11** | |

---

## 十、项目文件结构总览

```
novel-graphrag/
├── README.md                          # 项目说明（中英双语）
├── Dockerfile                         # Docker 构建
├── docker-compose.yml                 # 6 服务编排
├── nginx.conf                         # Nginx 反向代理
├── config.yaml                        # 统一配置
├── requirements.txt                   # 依赖清单
├── .env.template                      # 环境变量模板
├── .gitignore                         # Git 忽略规则
├── .venv.cmd                          # 虚拟环境激活脚本
│
└── novel_project/
    ├── run.py                         # FastAPI 启动入口
    ├── mcp_server.py                  # MCP Server (5 tools)
    │
    ├── core/
    │   ├── __init__.py
    │   ├── llm.py                     # LLM 统一调用层
    │   ├── chapter_parser.py          # Wiki 编译管道 (v2)
    │   ├── compiler_config.py         # 编译配置外置 🆕
    │   ├── chunker.py                 # 层级分块引擎
    │   ├── document_parser.py         # 文档解析引擎
    │   ├── knowledge_graph.py         # 图谱构建 + 别名消歧
    │   ├── retriever.py               # 3-level 检索 + 向量
    │   ├── memory.py                  # 会话记忆
    │   ├── pipeline.py                # 编译管道状态
    │   ├── security.py                # JWT/权限/限流
    │   ├── exporter.py                # 生态导出
    │   ├── chapter_router.py          # 分级路由 + 去重
    │   ├── slang_map.json             # 谐音黑话归一化
    │   └── agents/
    │       ├── coordinator.py         # 意图识别 + 任务分解
    │       ├── researcher.py          # 多源检索
    │       ├── writer.py              # 报告生成
    │       ├── structured_writer.py   # 结构化输出 + 锚点校验
    │       └── reviewer.py            # 质量审核
    │
    ├── interfaces/                    # 预留接口层 (6个)
    │   ├── portrait_generator.py
    │   ├── rlhf_pipeline.py
    │   ├── llm_provider.py
    │   ├── copyright_verifier.py
    │   ├── graph_storage.py
    │   └── eval_metrics.py
    │
    ├── scripts/
    │   ├── clean_novel.py             # TXT 清洗工具
    │   ├── rag_evaluate.py            # RAG 质量评估
    │   ├── migrate_to_neo4j.py        # Neo4j 迁移
    │   ├── test_agents.py             # Agent 测试
    │   ├── test_build_graph.py        # 图谱构建测试
    │   └── test_retriever.py          # 检索测试
    │
    ├── web/
    │   ├── app.py                     # FastAPI 主入口
    │   ├── routes/
    │   │   ├── agent_routes.py        # 图谱/检索/上传/编译 API
    │   │   └── auth_routes.py         # 登录/注册/权限 API
    │   ├── templates/
    │   │   ├── dashboard.html         # 首页（科技主题）
    │   │   ├── graph.html             # 图谱页（Cytoscape.js）
    │   │   ├── chat.html              # 问答页
    │   │   ├── upload.html            # 上传页
    │   │   └── login.html             # 登录页
    │   └── static/js/
    │       ├── auth.js                # 前端权限管理
    │       ├── cytoscape.min.js       # 图谱渲染引擎 (374KB)
    │       └── vis-network.min.js     # (已废弃，保留)
    │
    ├── tests/
    │   ├── __init__.py
    │   └── test_core.py               # 11 个单元测试
    │
    ├── data/
    │   ├── raw/                       # 原始 TXT
    │   ├── processed/                 # 清洗后 JSON
    │   ├── wiki/                      # 编译产物
    │   ├── chroma/                    # 向量索引
    │   ├── vocab/
    │   │   └── novel_domain.txt       # 领域分词词典
    │   ├── eval/golden/               # 黄金测试集
    │   └── checkpoints/              # 编译断点 🆕
    │       └── {novel_key}/
    │           ├── wiki_checkpoint.json
    │           ├── volume_checkpoint.json
    │           ├── book_checkpoint.json
    │           ├── graph_checkpoint.json
    │           ├── failed_chapters.json 🆕
    │           └── subchunks/         # 子块缓存 🆕
    │
    └── docs/
        ├── 前端分离与权限方案.md
        ├── 文档格式支持方案.md
        └── 项目完成清单.md            # ← 本文档
```

---

## 十一、数据文件清单

### 源数据

| 文件 | 大小 | 说明 |
|------|------|------|
| `data/raw/《绍宋》作者：榴弹怕水.txt` | 5.2MB | 原始 TXT |
| `data/raw/《斗破苍穹》作者：天蚕土豆.txt` | 85MB | 原始 TXT |
| `data/raw/《神印王座》作者：唐家三少.txt` | 40MB | 原始 TXT |
| `data/processed/《绍宋》作者：榴弹怕水.json` | 7.5MB | 清洗后 JSON |

### 编译产物（已清理，待重新编译）

| 文件 | 状态 | 说明 |
|------|------|------|
| `data/wiki/{novel}_wiki.json` | ⏳ 待重跑 | 章节 Wiki 条目 |
| `data/wiki/{novel}_hierarchical.json` | ⏳ 待重跑 | 三层 Wiki |
| `data/wiki/{novel}_graph.json` | ⏳ 待重跑 | 人物关系图谱 |

---

## 十二、已知未实现 / 待办

| 功能 | 优先级 | 说明 |
|------|--------|------|
| Word (.docx) 支持 | 🟢 P2 | `pip install python-docx` + 实现 DocxParser |
| PDF 支持（pdfplumber） | 🟢 P2 | 文字版 PDF 解析 |
| PDF 扫描件 OCR（PaddleOCR） | 🟡 P3 | 需要 paddlepaddle + 模型权重 |
| 前端置信度可视化 | 🟢 P2 | 问答页展示 faithfulness 分数 |
| 编译进度实时推送 | 🟢 P2 | WebSocket 或轮询 |
| Celery 异步任务 | 🟡 P3 | build_wiki 改为异步非阻塞 |
| 多用户数据隔离 | 🟡 P3 | 路径加 {user_id}/ 前缀 |
| Kubernetes 部署 | 🔴 P4 | |
| RLHF 微调闭环 | 🔴 P4 | 需 GPU |
| 本地 LLM 备胎 | 🔴 P4 | 需 GPU |
| 角色立绘 | 🔴 P4 | 需 GPU + IP-Adapter |
| 版权校验 | 🔴 P4 | 需商务合作 |

---

## 十三、统计汇总

| 维度 | 数值 |
|------|------|
| Python 文件数 | ~35 个 |
| 前端模板数 | 5 个 |
| API 路由数 | 18 个 |
| MCP Tools | 5 个 |
| 单元测试 | 11 个 |
| 预留接口 | 6 个 |
| 第三方依赖 | ~20 个 |
| 代码行数（估算） | ~15000+ 行 |
