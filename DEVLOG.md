# Novel-GraphRAG 开发日志

> 从零到一搭建网文知识图谱分析系统的全记录。
> 记录技术选型、踩坑经过、架构演进的真实思考。

---

## 目录

1. [起源：为什么要做这个项目](#1-起源)
2. [第一阶段：快速原型（2026-06-22）](#2-第一阶段)
3. [第二阶段：换掉图谱引擎（2026-06-30）](#3-第二阶段)
4. [第三阶段：编译管道重写（2026-07-05）](#4-第三阶段)
5. [第四阶段：性能与体验优化（2026-07-07）](#5-第四阶段)
6. [第五阶段：生产化改造（2026-07-09）](#6-第五阶段)
7. [技术选型复盘](#7-技术选型复盘)
8. [踩坑备忘录](#8-踩坑备忘录)

---

## 1. 起源

### 1.1 要解决的问题

网文爱好者（尤其是考据党、同人创作者）经常需要梳理小说中的人物关系。几百章的小说，人物动辄七八百个，关系上千条，靠人脑根本记不住。现有的 AI 问答产品（ChatGPT、DeepSeek 等）对长篇小说的人物关系理解很差，因为它们看不到全文，只能凭训练数据中的模糊印象回答。

### 1.2 核心思路

GraphRAG（图增强检索生成）——把小说先拆成章节，让 LLM 逐章提取人物和关系，构建知识图谱，问答时在图谱上做检索，再让 LLM 基于检索结果生成回答。这样 LLM 不需要记住全书，只需要读检索回来的那几段。

### 1.3 技术选型起点

```
后端: Python + FastAPI（熟悉、轻量）
前端: Jinja2 + Tailwind（快速出页面）
LLM: DeepSeek V4 Pro（性价比高，中文好）
图谱: NetworkX（简单够用）
向量: ChromaDB（本地优先，免运维）
```

选型原则：能本地跑就本地跑，尽量不依赖外部服务。

---

## 2. 第一阶段：快速原型

> 目标：跑通全链路，验证 GraphRAG 在网文场景是否可行

### 2.1 第一天做出来的东西

原型版本的核心链路：

```
上传 TXT → 按 3000 字截断 → LLM 提取 → 存 JSON → NetworkX 建图 → vis-network 展示
```

438 章的《绍宋》跑了大概一小时编译完，图谱出来了 759 个人物、1017 条关系。能看，但问题一大堆。

### 2.2 初期问题

| 问题 | 表现 | 原因 |
|------|------|------|
| 信息丢失 | 长章节只取了前 3000 字 | `chapter_text[:3000]` 硬截断 |
| 图谱卡顿 | 759 个节点缩放拖拽卡死 | vis-network 对大图性能差 |
| 节点重叠 | 所有节点挤成一团 | 力导向布局默认参数不适合大图 |
| 导出困难 | PNG 锯齿，SVG 要手动重建 | vis-network 原生导出弱 |
| 搜索结果差 | 「八公山」搜不到 | 只在摘要层匹配关键词，没搜原文 |

### 2.3 教训

> **"先跑通，再优化"是对的，但要分清楚哪些优化必须提前做。**

3000 字截断这种问题——跑通第一天就知道不对，但想着"后面再说"，结果后面重构的时候所有数据都要重跑。如果一开始就做分块，能省一次全量编译。

---

## 3. 第二阶段：换掉图谱引擎

> vis-network → Cytoscape.js

### 3.1 为什么换

vis-network 在大图（>500 节点）下性能急剧下降。759 个节点、1017 条关系，缩放拖拽都卡。更致命的是导出功能弱——PNG 要靠 canvas 截图，SVG 要手动用坐标重建。

Cytoscape.js 原生支持：

```javascript
cy.png({ scale: 4, bg: '#ffffff' })  // 一行代码导出 4K PNG
cy.svg()                                // 导出矢量 SVG
```

迁移成本不高，API 设计上两者很像。

### 3.2 布局调参的血泪史

这是花时间最多的部分。759 个节点，布局参数稍微不对就是灾难：

```
第一次：nodeRepulsion = 10000 → 全部重叠成一团
第二次：nodeRepulsion = 200000 → 炸成矩形（节点被推到画布边缘）
第三次：nodeRepulsion = 500000 → 矩形更明显
最终：nodeRepulsion = 自定义公式 + 布局后强制分离迭代 → 终于能看了
```

**核心教训**：Cytoscape.js 的 `nodeOverlap` 和 `minNodeSpacing` 参数在 `cose` 布局中是**不生效的**。文档没写，试了才知道。真正的解决方案是布局完成后自己写一个节点分离迭代：

```javascript
for (let iter = 0; iter < 40; iter++) {
  for (let i = 0; i < nodes.length; i++) {
    for (let j = i + 1; j < nodes.length; j++) {
      // 如果两个节点距离小于阈值，互相推开
    }
  }
}
```

### 3.3 自适应布局参数

不同规模的小说需要的布局参数完全不同（50 节点 vs 759 节点 vs 3000 节点）。写了一个后端公式根据节点数和边数动态计算：

```python
repulsion = int(min(100000 * (n / 100) ** 2.0, 3000000))
gravity = round(max(0.003, min(0.08, 0.04 * (100 / n) ** 0.4)), 4)
```

这样切换书籍时图谱参数自动适配，不用手动调。

---

## 4. 第三阶段：编译管道重写

> 从「能跑」到「能可靠地跑」

### 4.1 旧管道的六个问题

在跑完《绍宋》全量编译后，仔细审查了代码，发现了六个隐患：

1. **断点续传靠长度判断**——如果某一章半写入，重启直接跳过
2. **长章节按字符硬切**——对话和事件被切到两个块里
3. **并发控制缺失**——串行调用 LLM，几百章跑得慢
4. **失败直接崩溃**——LLM 返回空 JSON 直接中断编译
5. **没有 Token 预算**——跑完才知道花了多少
6. **配置硬编码**——2800 字分块、50 章一卷全写死在代码里

全部重写花了大概两天。改动最大的部分：

### 4.2 断点续传重写

```python
# 旧的（有 bug）
if os.path.exists(checkpoint_path):
    wiki_entries = json.load(f)
    resume_from = len(wiki_entries)  # 只靠长度判断

# 新的
cpm = CheckpointManager(novel_key)
completed = cpm.get_completed("wiki")  # 用 chapter_id 集合
```

同时加了原子写入（临时文件 → `os.replace` → 覆盖原文件），避免半残文件。

### 4.3 语义分块

按段落/对话边界切割，而不是字符长度：

```python
paragraphs = [p.strip() for p in re.split(r"\n\s*\n", text) if p.strip()]
```

每块保证是一个完整的段落，不会切断对话和剧情。

### 4.4 子块级断点

这是我自己最满意的一个设计。超长章节拆成多个子块，每块提取完成就落地缓存：

```
长章 → 切成 5 块 → 第 1 块完成（缓存）→ 第 2 块完成（缓存）→ ...
                                                          ↓ 如果此时崩溃
                                                          → 下次只重跑 3~5 块
```

相比之前「一章崩溃整章重跑」，省了 60-70% 的 Token。

---

## 5. 第四阶段：性能与体验优化

### 5.1 回答太慢的问题

用户问一个问题等 25-35 秒，这是最影响体验的。链路：

```
意图识别 (3s) → 任务拆解 (3s) → 检索 (2s) → 写报告 (10s) → 审核 (5s)
                                                                 ↓ 不通过来一轮
```

改了三个地方：

1. **首轮跳过审核**——省 5 秒
2. **语义缓存**——相似问题直接返回，省 25 秒
3. **检索附加向量搜索**——不依赖关键词匹配，搜原文全文

改完后，命中缓存的问题 50ms 返回，没命中的 15-25 秒。

### 5.2 语义缓存的设计

用 ChromaDB 内置的 ONNX embedding（零网络依赖，纯本地）：

```python
class SemanticCache:
    def get(self, query):
        vec = embed(query)
        for entry in self._entries:
            if cosine_similarity(vec, entry.embedding) > 0.85:
                return entry.answer
        return None
```

阈值 0.85 是试出来的——太低会误匹配，太高又变成精确匹配失去语义意义。

### 5.3 片段式材料管理

多轮问答时，旧材料不断累积。第一轮的 2000 字 + 第二轮的 2000 字 + 第三轮的 2000 字 → LLM 上下文爆炸。

解决方案：旧材料 LLM 压缩为 200 字摘要，只保留最近两轮全量。压缩本身也要 LLM 调用，但压缩 2000 字到 200 字的成本远低于直接用全量导致的 Token 膨胀。

---

## 6. 第五阶段：生产化改造

### 6.1 权限系统

JWT + 4 角色（admin/editor/viewer/api），`BaseHTTPMiddleware` 做全局拦截，前端 `data-perm` 属性控制显隐。代码早就写好了，但一直没注册到 `app.py`——注册上线后匿名用户直接 401，才发现好多内部调用没传 token。

### 6.2 DB 抽象层

前后端分离需要用户数据持久化。不想一上来就上 MySQL（太重），但也不想像原来那样直接读写 JSON 文件（没法扩展）。设计了一个抽象接口：

```
DBBackend (接口)
  ├── JsonBackend (当前，JSON 文件)
  └── MySQLBackend (桩代码，迁移时实现)
```

调用方不关心后端是什么，统一 `get_db().get_user()`。以后迁移 MySQL 只需要写一个实现类，调用方零改动。

### 6.3 跨书检索

三本书都编译完后，用户想跨书搜一个人物。逐本遍历 JSON 太慢，于是建了一个倒排索引：

```
关键词 → [(书, 章节), ...]
```

索引构建花几秒，搜索毫秒级。只索引了摘要和人物名，没索引全文（太大了），但已经够用。

---

## 7. 技术选型复盘

### 做对了的

| 选型 | 理由 | 验证 |
|------|------|------|
| **FastAPI** | 异步、类型安全、自动文档 | 全部符合预期 |
| **Cytoscape.js** | 大图性能好、导出原生 | 比 vis-network 好太多 |
| **ChromaDB** | 本地优先，零运维 | 内置 ONNX embedding 免部署 |
| **DeepSeek API** | 性价比高 | 跑完全书编译不到 $1 |
| **Tailwind CSS** | 快速出界面 | 配合科技主题效果不错 |

### 做错了或者有遗憾的

| 决策 | 问题 | 如果重来 |
|------|------|---------|
| **一开始用 vis-network** | 大图性能差，白花两天时间 | 直接上 Cytoscape.js |
| **3000 字截断** | 导致全量数据需要重跑 | 一开始就做分块 |
| **按长度断点** | 脏数据风险 | 一开始就用 chapter_id 集合 |
| **串行编译** | 438 章跑一小时 | 直接上 ThreadPoolExecutor |
| **没有早期加向量检索** | 关键词搜索严重漏召回 | 一开始就加上 |

### 应不应该用 GraphRAG？

对于网文这个场景，GraphRAG 的优势是能处理「跨章节的人物关系推理」。如果只是简单的 QA（「第一章讲了什么」），纯 RAG 就够了。但用户问「赵玖和岳飞的关系变化」，需要把多个章节的实体关系合并后才能回答，这就是 Graph 的价值。

**结论**：对于人物关系密集的场景，GraphRAG 比纯 RAG 好；但实现复杂度也高一个量级。

---

## 8. 踩坑备忘录

### 8.1 Windows 下你不知道的事

- `.venv/Scripts/activate` 在 PowerShell 里报执行策略错误——用 `cmd /c` 或直接用 `.venv/Scripts/python`
- `netstat -ano | findstr :8000` 比 `lsof` 难用十倍
- 路径反斜杠在 Git Bash 里经常要转义
- `taskkill /F /IM python.exe` 杀进程必备

### 8.2 LLM 的奇怪行为

- 让 LLM 输出 JSON 时，偶尔会多一段「好的，这是您需要的 JSON：」——必须用 `response_format={"type": "json_object"}` 强制约束
- Faithfulness 评估时，LLM 倾向于给高分（70% 以上的大部分 0.8+），真正错的反而是那些看起来像对的
- Writer prompt 里写了「如果资料不足，如实说明」，LLM 直接每篇报告末尾加一段免责声明——删掉后效果立刻好了

### 8.3 图谱布局的玄学

- `nodeRepulsion` 和 `gravity` 需要同时调，只调一个没用
- 759 节点以下，`cose` 布局够用；超过 1000 节点，必须用 `cose-bilkent`
- 布局后的节点分离迭代（40 轮、阈值 20px、力度 0.6）是大图不重叠的关键，比调任何布局参数都有用
- 不要依赖 `minNodeSpacing` 和 `nodeOverlap`——Cytoscape.js 文档没写但这两个在很多布局里不生效

### 8.4 数据

- 三本书的全量编译大概花了 6-8 小时（串行 + API 延迟）
- 全是编完才发现 prompt 写错了要重跑——先跑 5 章验证，再跑全量
- 语义缓存文件 `data/cache/semantic_cache.json` 会随着使用增长，2000 条上限是够用的
- ChromaDB 的索引目录 `data/chroma/` 大概 100MB，可以放进 `.gitignore`

---

## 致谢

这个项目的完成离不开 DeepSeek API 的稳定服务。所有 LLM 调用（编译、问答、评估）都由 DeepSeek V4 Pro 完成，全文编译三本书的总花费不到 3 美元。

---

> 最后更新：2026-07-09
> 作者：yekup
